import Ember from 'ember';

var ProfileConnectionsView = Ember.View.extend({
    tagName: 'section',
    elementId: 'page',
    classNames: ['connections']
});

export default ProfileConnectionsView;
